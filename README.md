To run this system, clone this repository and then add it in the xampp folder, 
then loacte the folder and then open the file in your text editor e.g VS code or Pycharm,
start your xampp php server and appache, create the db called school_db and then run 
open the folder in your local browser .
create the tables by locating the congig/create_tables.php to create the tables.
